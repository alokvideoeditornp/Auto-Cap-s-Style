const puppeteer = require("puppeteer"); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto("file://" + __dirname + "/test.html"); const box = await page.evaluate(() => { const el = document.getElementById("test"); return { height: el.offsetHeight, child1: el.children[0].offsetHeight, child2: el.children[1].offsetHeight }; }); console.log(box); await browser.close(); })();